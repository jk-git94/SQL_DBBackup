git init
git push origin master --force
git config --global user.email "junaid.khalfay@arconnet.com"
git commit -m "initial commit"
git push origin master --force
git clone https://github.com/jk-git94/SQL_DBBackup.git
